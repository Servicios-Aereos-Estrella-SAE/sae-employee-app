/**
 * Propiedades del layout principal
 * @interface
 * @property {React.ReactNode} children - Componentes hijos del layout
 */
export interface ILayoutProps {
  /**
   * Componentes hijos del layout
   * @type {React.ReactNode}
   */
  children: React.ReactNode
}
