// import { IUser } from '../types/user.interface';
// import { LoginCredentials } from '../models/User';

export interface UserRepository {
  // changePassword(password: string): Promise<void>;
  // login(credentials: LoginCredentials): Promise<{ user: IUser; token: string }>;
  // saveAuthState(token: string, user: IUser): Promise<void>;
  // getAuthState(): Promise<{ token: string | null; user: IUser | null }>;
  // clearAuthState(): Promise<void>;
  // clearFullAuthState(): Promise<void>;
  // isBiometricAvailable(): Promise<boolean>;
  // authenticateWithBiometrics(): Promise<boolean>;
  // saveCredentials(userEmail: string, userPassword: string): Promise<void>
}
